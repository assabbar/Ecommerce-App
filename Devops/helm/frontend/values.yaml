replicaCount: 1
image:
  repository: acrecomdev12231416.azurecr.io/frontend
  pullPolicy: IfNotPresent
  tag: "latest"
imagePullSecrets:
  - name: acr-credentials
serviceAccount:
  name: "frontend-sa"
service:
  type: LoadBalancer
  port: 80
  targetPort: 80
resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 30m
    memory: 64Mi
livenessProbe:
  httpGet:
    path: /
    port: 80
  initialDelaySeconds: 30
readinessProbe:
  httpGet:
    path: /
    port: 80
  initialDelaySeconds: 15
env:
  - name: API_GATEWAY_URL
    value: "http://api-gateway.backend.svc.cluster.local:8080"
autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80
