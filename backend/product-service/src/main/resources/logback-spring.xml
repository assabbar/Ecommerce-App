<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Use Spring Boot's default logging configuration (CONSOLE appender) -->
    <include resource="org/springframework/boot/logging/logback/base.xml"/>
    <!-- Loki is disabled - logs are sent to stdout and captured by Kubernetes -->
</configuration>